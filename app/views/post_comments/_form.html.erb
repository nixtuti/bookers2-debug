<%= form_with (model: [book, post_comment], local: false) do |f| %>
  <%= f.text_area :comment, class: "form-control", rows: '5', placeholder: 'コメントをここに' %><br>
  <%= f.submit "送信", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>